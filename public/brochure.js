(()=>{const section=document.querySelector('#recipe-brochure'),stage=section.querySelector('.brochure-stage');const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;let queued=false;function render(){queued=false;const w=stage.clientWidth,h=stage.clientHeight;section.style.setProperty('--brochure-scale',Math.min((w-32)/1200,(h-80)/600));const progress=Math.max(0,Math.min(1,-section.getBoundingClientRect().top/(section.offsetHeight-h)));const t=Math.min(1,progress/.72);const eased=t*t*(3-2*t);section.style.setProperty('--fold',(reduced?0:88*(1-eased))+'deg')}function update(){if(!queued){queued=true;requestAnimationFrame(render)}}addEventListener('scroll',update,{passive:true});addEventListener('resize',update);render()})();